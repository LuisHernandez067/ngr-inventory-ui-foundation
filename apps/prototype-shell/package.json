{
  "name": "@ngr-inventory/prototype-shell",
  "version": "0.0.1",
  "description": "Prototipo navegable de NGR Inventory — app Vite + Bootstrap Theme",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --open",
    "dev:chrome": "BROWSER=google-chrome vite --open",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@ngr-inventory/api-mocks": "*",
    "@ngr-inventory/bootstrap-theme": "*",
    "@ngr-inventory/design-tokens": "*",
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "msw": "^2.7.0",
    "sass": "^1.83.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.0",
    "vitest": "^2.1.8"
  }
}
