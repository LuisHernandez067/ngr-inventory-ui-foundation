// Módulo de footer mínimo — muestra información de versión del prototipo

/**
 * Renderiza el HTML del footer del área de contenido principal.
 */
export function render(): string {
  return `
    <footer class="layout-footer">
      <small>NGR Inventory v0.1.0 — Prototipo</small>
    </footer>
  `;
}
